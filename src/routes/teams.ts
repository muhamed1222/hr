import express, { Router } from 'express';

const router: Router = express.Router();

// Placeholder for team routes
router.get('/', (req, res) => {
  res.json({ message: 'Teams API - Coming soon' });
});

export default router; 